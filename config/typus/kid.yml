# Use the README file as a reference to customize settings.

Kid:
  fields:
    default: status, name, address, age, video, description
    form: status, name, address, age, video, description
  relationships: user_kids, users
  application: Application
